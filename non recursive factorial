#include <stdio.h>
int fact(int n);
void main ()
{
    int n,f;
    printf("enter the number\n");
    scanf("%d",&n);
    f=fact(n);
    printf("factorial is %d\n",f);
}
int fact(int n)
{
int i,factorial=1;
for (i=1;i<=n;i++)
{
    factorial=factorial*i;
}
return factorial;
}
